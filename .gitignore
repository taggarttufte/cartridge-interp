# Copyrighted source text — NEVER commit
data/
*.epub
*.txt

# Private drafts (not for publication)
aaron_email.md

# Vendored third-party reference code — install from upstream instead (see README)
ref_*

# Model checkpoints / cart weights / saved arrays
*.pt
*.bin
*.safetensors
*.npy
*.npz
*.ckpt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Internal working docs — keep local, NOT in the public repo
HANDOFF.md
TRIGGER_CART_PLAN.md
NOTES_personal.md
NEXT_ARC_PLAN.md
CAS_POISON_PLAN.md
CAMPAIGN_LOG.md
# Blog draft — hold local until ready to ship
blog/

# stray binaries
*.tar.gz

# Operational / cloud-helper scratch — never public
*.sh
*.ps1
scripts/_*.py
output_cloud/
output/
output_wsl_backup/
*.log

# Grey — contains copyrighted Shadow Slave content; NEUTRALIZE before any public push (handle next session)
scripts/cart_quiz.py
QUIZ_DESIGN.md
